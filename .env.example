# Example env for apps/web. Do NOT commit real secrets. Use GitHub Secrets for CI.
# Local env variables you might need for local testing (example placeholders):

# NOTE: Cloudflare deployment configuration has been removed from this repo.
# If you later decide to re-enable Cloudflare Pages, contact the maintainer
# and they will help add secure deployment configuration and required secrets.

# Parent origin to use when sending window.postMessage from embedded frames
# Example: https://your-sandbox-host.example.com
NEXT_PUBLIC_PARENT_ORIGIN=
## Google Tag Manager (put your GTM container id here, e.g. GTM-XXXXXXX)
VITE_GTM_ID=GTM-5P58Q96W
# Example environment variables
# Copy this to .env and fill values locally

# Server-side EmailJS (preferred) — EmailJS server-side credentials. Use these for the server-only flow.
EMAILJS_USER_ID=your_emailjs_user_id_here
EMAILJS_SERVICE_ID=your_emailjs_service_id_here
EMAILJS_TEMPLATE_ID=your_emailjs_template_id_here

# SendGrid (server-side transactional email provider)
SENDGRID_API_KEY=your_sendgrid_api_key_here
SENDGRID_FROM=your@example.com
SENDGRID_FROM_NAME=Your Name or Service

# Rate limiting (values are per-process; use a shared cache or external rate limiter in production)
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX=10

# Deliverability / DNS (set in your DNS provider, not in env):
# - SPF record example: v=spf1 include:_spf.sendgrid.net ~all
# - DKIM: configure using provider's DKIM TXT records
# - DMARC: v=DMARC1; p=quarantine; rua=mailto:postmaster@endonezyakasifi.com
