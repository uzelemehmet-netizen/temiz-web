// Minimal dev error overlay loader
// The real project injects a richer overlay. For local preview we keep it tiny
// and non-blocking.
(() => {
  // no-op unless we decide to inject a UI later
})();
